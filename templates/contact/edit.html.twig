{% extends 'base.html.twig' %}

{% block body %}
    <h1>Редактирование контакта</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Сохранить</button>
    <a href="{{ path('contact_delete', {'lastName': contact.lastName}) }}" class="delete-contact-button">Удалить контакт</a>
    {{ form_end(form) }}
{% endblock %}